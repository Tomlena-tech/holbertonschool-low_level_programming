#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
/**
* main - Prints a text according number
* Return: Always (Success)     
*/
int main(void)
{

  char letters = 'a';
  while (letters = 'z'; letters <= 'z'; letters++)
    putchar(letters);
    if letters == 'e' or letters == 'q':
       continue
	 putchar(letters);



}
