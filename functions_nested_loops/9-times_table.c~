#include "main.h"
#include <unistd.h>


void times_table(void)
{
int first_number;
int second_number;

for (first_number = 0; first_number =< 9; first_number++)
  { _putchar ('0'+ first_number);
 for (second_number = 0, second_number =<9; second_number++)
   _putchar ('0' + second_number)
     _putchar result (first_number * second_number);
